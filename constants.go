package srtconv

// SrtFormatErr is returned by the srt parser when srt is invalid
const SrtFormatErr = "invalid srt format"

// CaptionFormatErr is returned by caption parser when parts of a caption is invalid
const CaptionFormatErr = "invalid caption format"

// TimeFormatErr is returned by the time parser when a time is invalid
const TimeFormatErr = "invalid time format"
